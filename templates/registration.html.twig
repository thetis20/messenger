{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <Button type="submit" class="btn btn-success">Subscribe</Button>
    {{ form_end(form) }}
{% endblock %}
